apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: autocare-secret-store
  namespace: autocare
spec:
  provider:
    aws:
      service: SecretsManager
      region: <AWS_REGION>
      auth:
        jwt:
          serviceAccountRef:
            name: autocare-sa
