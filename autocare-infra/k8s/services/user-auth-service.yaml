apiVersion: v1
kind: Service
metadata:
  name: user-auth-service
  namespace: autocare
  labels:
    app: user-auth-service
spec:
  type: ClusterIP
  selector:
    app: user-auth-service
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
