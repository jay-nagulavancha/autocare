apiVersion: v1
kind: ConfigMap
metadata:
  name: vehicle-maintenance-service-config
  namespace: autocare
data:
  DB_PORT: "3306"
  DB_NAME: maintenance_db
  DB_USERNAME: root
  # Shown in UI via GET /api/v1/version — bump with your release tag / commit when you deploy.
  AUTOCARE_GIT_COMMIT: "ce099046d7d5abffdb004b27fece414997bc726d"
