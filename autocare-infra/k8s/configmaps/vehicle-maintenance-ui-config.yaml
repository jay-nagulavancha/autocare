apiVersion: v1
kind: ConfigMap
metadata:
  name: vehicle-maintenance-ui-config
  namespace: autocare
data:
  # Use relative paths so ALB DNS rotations do not require config updates.
  VITE_AUTH_API_URL: "/api/auth"
  VITE_MAINTENANCE_API_URL: "/api/v1"
